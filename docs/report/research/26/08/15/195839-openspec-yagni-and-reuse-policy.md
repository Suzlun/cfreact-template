# 調査報告: OpenSpecのYAGNIと再利用優先方針

## 基本情報

- 調査日時: 2026-08-15 19:58:39 +0000
- 調査者: `researcher`
- 依頼概要: ProposerとReviewerが顧客価値と手段を分離し、設計を伴うChangeで既存実装と外部パッケージの再利用を優先できるようにする。
- 調査範囲: `cfreact-template` のOpenSpec補足スキル、変更スキーマ、設計テンプレート、決定的検査、人向け運用資料。
- 調査時点のリポジトリ: `main`、`ace107f`

## 結論

現行運用は成果と手段の分類、および仕様からパッケージや実装方式を排除する基本境界を持つ一方、RequirementとScenarioを根拠のある顧客価値だけに限定する明示規則、設計前の再利用調査、`design.md`への調査結果と採否の記録、Reviewerによる再利用不足の拒否が不足している。意味上の顧客価値と手段の判定は既存の共有レビュー契約へ、再利用証拠の構造は既存の設計検査へ追加することで、新しい検査経路を増やさずに要望を満たせる。

## 既存調査の確認

| 既存報告 | 関連性             | 鮮度・変化頻度               | 現在の情報との整合性 | 再検証結果                                                           | 採否     |
| -------- | ------------------ | ---------------------------- | -------------------- | -------------------------------------------------------------------- | -------- |
| なし     | 同題の既存報告なし | リポジトリ構造は変更され得る | 該当なし             | `docs/report/research/**` を検索し、README以外に報告がないことを確認 | 該当なし |

## 調査方法

OpenSpecのProposer・Reviewer定義、その共有補足スキル、両変更スキーマ、`architecture-change` の設計テンプレート、設計と作業範囲の検査および試験、一次運用資料を読み、生成物との境界と既存の責務分担を照合した。ウェブ調査は行っていない。

## 確認済みの事実

| 事実                                                                                                 | 根拠                                                                                                                                         | 確認日時   | 情報の消費期限・変化要因                                     |
| ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------ |
| Proposerの詳細運用は生成スキルではなくリポジトリ固有の補足スキルが所有する                           | `.opencode/agents/openspec/proposer.md:158-163`、`.opencode/skills/openspec/openspec-proposer-workflow/SKILL.md:9-14`                        | 2026-08-15 | エージェント構成またはOpenSpec生成方針の変更時に再確認が必要 |
| Reviewerの意味検査は共有の`openspec-review`契約が所有する                                            | `.opencode/agents/openspec/reviewer.md:153-155`、`.opencode/skills/openspec/openspec-review/SKILL.md:9-21`                                   | 2026-08-15 | レビュー委譲構造の変更時に再確認が必要                       |
| 現行仕様規則は観測可能な結果だけを許すが、顧客価値の根拠と希望体験を表すUI構成の境界を明示していない | `openspec/config.yaml:42-46`、`openspec/schemas/behavior-change/schema.yaml:27-33`、`openspec/schemas/architecture-change/schema.yaml:23-26` | 2026-08-15 | スキーマ更新時に再確認が必要                                 |
| 現行`design.md`には再利用候補、採否、独自実装の根拠を記録する節がない                                | `openspec/schemas/architecture-change/templates/design.md:1-41`                                                                              | 2026-08-15 | 設計テンプレート更新時に失効                                 |
| 既存検査は`design.md`の見出し順を決定的に検査している                                                | `scripts/openspec/verify-change-task-scope.mjs:15-26`、`scripts/openspec/verify-change-task-scope.mjs:205-224`                               | 2026-08-15 | 検査実装更新時に再確認が必要                                 |

## 推論と判断

| 推論・判断                                                                                             | 根拠となる事実                                                       | 前提                                                         | 確信度 |
| ------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------- | ------------------------------------------------------------ | ------ |
| 顧客価値と手段の区別は正規表現ではなくReviewerの意味検査で扱うべき                                     | 自然言語上の規格名やパッケージ名は文脈により要望にも説明にも現れ得る | 誤検出を避ける必要がある                                     | 高     |
| 再利用調査表の必須列と実データ行は既存設計検査で扱うべき                                               | 同検査がすでに設計見出しを所有する                                   | 構造検査は決定的に実行できる                                 | 高     |
| ProposerはChange成果物の確定前に既存コード、導入済みパッケージ、実績ある外部パッケージの順で調査すべき | クレドの再利用順序と依頼内容                                         | セキュリティ、サプライチェーン、アーキテクチャ規則を優先する | 高     |

## 矛盾・不確実性

`AGENTS.md`は原則として英語で記述する規則を持つ一方、クレド自体は所有者の指示により日本語で記述されている。今回の編集対象は既存方針どおり、OpenCode定義とスキルを英語、スキーマ・テンプレート・日本語資料を日本語とする。外部パッケージの具体的な選定は個別Changeごとの調査対象であり、本調査では評価していない。

## 推奨事項

ProposerとReviewerの共有補足スキル、両スキーマの仕様指示、`architecture-change`の設計指示とテンプレート、既存設計検査とその少数の強い試験、一次運用資料を同期する。生成された`opsx-*`コマンドと`openspec-*`スキルは手編集しない。

## 出典

### ウェブ

- なし（理由: リポジトリ内の運用契約と実装だけで判断できるため）

### リポジトリ

- `.opencode/agents/openspec/proposer.md:156-176`（`main`、`ace107f`）: Proposer本体の委譲境界。
- `.opencode/agents/openspec/reviewer.md:151-169`（`main`、`ace107f`）: Reviewer本体の委譲境界。
- `.opencode/skills/openspec/openspec-proposer-workflow/SKILL.md:23-149`（`main`、`ace107f`）: 分類、成果物作成、収束手順。
- `.opencode/skills/openspec/openspec-review/SKILL.md:23-75`（`main`、`ace107f`）: 目的と手段、意味レビュー手順。
- `openspec/schemas/architecture-change/schema.yaml:18-42`（`main`、`ace107f`）: 仕様と設計の指示。
- `openspec/schemas/architecture-change/templates/design.md:1-41`（`main`、`ace107f`）: 現行設計テンプレート。
- `scripts/openspec/verify-change-task-scope.mjs:15-26`、`scripts/openspec/verify-change-task-scope.mjs:205-224`（`main`、`ace107f`）: 設計構造の決定的検査。

## 調査ログ

1. `docs/report/research/**`を検索し、同題の既存報告がないことを確認した。
2. ProposerとReviewerのエージェント定義から、共有補足スキルへの委譲境界を確認した。
3. 両変更スキーマとテンプレートを比較し、顧客価値の根拠と再利用評価の欠落を確認した。
4. 既存の設計検査と試験を読み、新しい検査器を追加せず拡張できることを確認した。
5. 生成ファイルを特定し、今回の手編集対象から除外した。

## 残存課題

なし（理由: 個別Changeでの外部パッケージ選定を除き、今回の運用改修に必要な責務と編集境界を確認できたため）
