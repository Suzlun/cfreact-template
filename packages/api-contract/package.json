{
  "name": "@cfreact-template/api-contract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "tsp format .",
    "format:check": "tsp format . --check",
    "gen:openapi": "tsp compile .",
    "check": "pnpm format:check && tsp compile . --no-emit"
  },
  "devDependencies": {
    "@typespec/compiler": "^1.8.0",
    "@typespec/http": "^1.8.0",
    "@typespec/openapi": "^1.8.0",
    "@typespec/openapi3": "^1.8.0"
  }
}
