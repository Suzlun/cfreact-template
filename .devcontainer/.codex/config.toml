# Codex CLI MCP Server Configuration

[mcp_servers.serena]
command = "uvx"
args = [
    "--from", "git+https://github.com/oraios/serena",
    "serena", "start-mcp-server",
    "--context", "codex",
    "--project", "/workspaces/cfreact-template"
]
